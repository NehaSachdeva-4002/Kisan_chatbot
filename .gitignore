# Python virtual environment
venv/
.venv/
env/
.env/
llm_env/

# Cached Python files
__pycache__/
*.pyc
*.pyo
*.pyd

# BERTopic and FAISS cached files
document_embeddings.npy
bertopic_model.pkl
faiss_index.bin
topic_ids.pkl
topic_docs.pkl

# IDE specific files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# OS generated files
.DS_Store
Thumbs.db

# Flask specific
instance/
.flaskenv

# DANGER: If you hardcoded your API key, make sure you delete that line from app.py
#         before committing if you intend to make the repo public.
#         If app.py has the key AND you want to make the repo public, you MUST
#         either remove the key from app.py or add app.py to .gitignore (but then
#         app.py itself won't be version controlled).